%p.exhibit-tally
  Exhibiting
  %span.count= pluralize(@galleries.total_count, 'Gallery')

%ul.thumbs
  - @galleries.each do |g|
    %li
      .image-container
        = link_to image_tag(g.thumb_url, class: 'thumb'), g
      .caption= link_to g.title, g

= paginate @galleries
