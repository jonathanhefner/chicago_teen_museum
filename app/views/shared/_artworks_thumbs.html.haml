%ul.thumbs
  - artworks.each do |a|
    %li
      .image-container
        = link_to image_tag(a.thumb_url, class: 'thumb'), a
      .caption
        %h3= link_to a.title, a
        %p.byline
          by
          = link_to a.user